Array - I
Problem - 1 link - https://www.naukri.com/code360/problems/reverse-the-array_1262298
Solution - 
#include <bits/stdc++.h>
using namespace std;

void reverseArray(vector<int> &arr, int m) {
    int left = m+1;
    int right = arr.size() - 1;
    while(left<right){
        swap(arr[left],arr[right]);
        left++;
        right--;
    }
} // solved without using stl library
